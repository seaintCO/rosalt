import {createSupabaseAdmin} from "./supabase/admin";import {env} from "./env";
async function digest(value:string){const data=new TextEncoder().encode(value);const hash=await crypto.subtle.digest("SHA-256",data);return Array.from(new Uint8Array(hash),x=>x.toString(16).padStart(2,"0")).join("")}
export async function rateLimit(request:Request,bucket:string,limit:number,windowSeconds:number){if(!env.SUPABASE_SERVICE_ROLE_KEY)return true;const forwarded=request.headers.get("x-forwarded-for")?.split(",")[0]?.trim()??"unknown";const key=await digest(`${bucket}:${forwarded}:${request.headers.get("user-agent")??""}`);const {data,error}=await createSupabaseAdmin().rpc("consume_rate_limit",{p_key_hash:key,p_bucket:bucket,p_limit:limit,p_window_seconds:windowSeconds});if(error)throw error;return Boolean(data)}
export function assertSafeExternalUrl(value:string){const url=new URL(value);if(url.protocol!=="https:")throw new Error("HTTPS_REQUIRED");const h=url.hostname.toLowerCase();if(h==="localhost"||h.endsWith(".local")||h==="0.0.0.0"||h==="127.0.0.1"||h==="::1"||/^10\./.test(h)||/^192\.168\./.test(h)||/^172\.(1[6-9]|2\d|3[01])\./.test(h)||/^169\.254\./.test(h))throw new Error("PRIVATE_ADDRESS_BLOCKED");return url}
export async function encryptSecret(value:string){if(!env.TOKEN_ENCRYPTION_SECRET)throw new Error("ENCRYPTION_SETUP_REQUIRED");const iv=crypto.getRandomValues(new Uint8Array(12));const keyMaterial=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(env.TOKEN_ENCRYPTION_SECRET));const key=await crypto.subtle.importKey("raw",keyMaterial,"AES-GCM",false,["encrypt"]);const encrypted=await crypto.subtle.encrypt({name:"AES-GCM",iv},key,new TextEncoder().encode(value));return `${Buffer.from(iv).toString("base64url")}.${Buffer.from(encrypted).toString("base64url")}`}
export async function fingerprint(value:string){return digest(value)}
