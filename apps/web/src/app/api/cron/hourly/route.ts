import {env} from "@/lib/env";import {ok,problem} from "@/lib/http";import {createSupabaseAdmin} from "@/lib/supabase/admin";
export async function GET(request:Request){const ref=crypto.randomUUID();if(!env.CRON_SECRET||request.headers.get("authorization")!==`Bearer ${env.CRON_SECRET}`)return problem("UNAUTHORIZED","Valid cron authorization is required.",401,ref);const {data,error}=await createSupabaseAdmin().rpc("expire_stale_records");if(error)return problem("CRON_FAILED","Hourly maintenance failed.",500,ref);return ok({completedAt:new Date().toISOString(),expired:data},200,ref)}
